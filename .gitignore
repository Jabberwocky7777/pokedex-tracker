# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Windows local launchers (not needed in repo)
*.exe
*.vbs
launcher/

# Claude Code session files
.claude/

# Sync runtime config (local dev only — never commit a real token)
public/env.js

# Sync server local data
sync-server/node_modules/

# Docker env file (contains secrets)
.env

# Capacitor build output (built in CI — do not commit)
ios/build/
assets/
